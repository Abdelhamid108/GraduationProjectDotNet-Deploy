---
# This playbook's job is to install Docker.
 - name: install docker in all nodes 
   # It targets 'all' hosts defined in the inventory (masters and workers).
   hosts: all
   become: true  
   gather_facts: no # Saves time by not re-gathering facts
   roles:
     # It executes the tasks defined in the 'Docker_installation' role.
     - ../roles/Docker_installation

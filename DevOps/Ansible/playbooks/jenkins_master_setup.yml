---
# This playbook installs and configures the Jenkins primary instance.
- name: setup jenkins master 
  # It targets *only* the hosts in the [masters] group.
  hosts: all
  gather_facts: no
  roles:
    # It executes the tasks defined in the 'jenkins_master_setup' role.
    - jenkins_master_setup
